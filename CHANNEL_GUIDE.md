# 📱 채널 기반 텔레그램 시스템 사용 가이드

## 🎯 개요

EXIT COMPANY 알림 채널에서 **모든 기능을 직접 실행**할 수 있습니다. 별도 봇에게 DM을 보낼 필요가 없습니다!

## ✅ 설정 완료 후 사용법

### 1️⃣ 채널에서 명령어 입력

채널에 직접 메시지를 입력하면 Admin Bot이 자동으로 응답합니다:

```
EXIT COMPANY 알림 채널에서:

/status
→ 📊 전체 티켓 현황 표시

/pending
→ ⏳ 대기중인 승인 목록 표시

/transactions
→ 💰 오늘 입출금 내역 표시

/unconfirmed
→ 💳 미확인 입금 목록 표시

/settle
→ 💰 배팅 자동 정산 실행

/bookkeep
→ 📚 장부 자동 정리 실행

/help
→ 📱 사용 가능한 명령어 목록
```

### 2️⃣ 자동 파싱

Parser Bot을 채널에 추가하면 입출금 메시지를 자동으로 파싱합니다:

```
채널에 메시지 입력:

입금 1,000,000원 홍길동
→ 💵 입금 감지 알림 + DB 저장

출금 500,000원 김철수
→ 💸 출금 감지 알림 + 출처 확인 버튼 표시
```

### 3️⃣ 인라인 버튼 사용

출처 미확인 출금 시 채널에 버튼이 표시됩니다:

```
💸 출금 감지

금액: 300,000원
출금자: 김철수

⚠️ 출처 미확인 - 출금 사유 확인 필요

[💼 업무 경비] [🏦 회원 출금]
[📦 물품 구매] [🔧 기타]

→ 직원이 버튼 클릭하면 자동으로 출처 기록
```

### 4️⃣ 승인 처리

승인 요청도 채널에서 버튼으로 처리:

```
⚠️ 승인 요청

유형: 입금
회원: 홍길동
금액: 1,000,000P

[✅ 승인] [❌ 거절]

→ 관리자만 버튼 클릭 가능
```

## 🔧 설정 방법

### Step 1: Admin Bot 채널 추가 (필수)

```
1. EXIT COMPANY 알림 채널 열기
2. 채널 정보 → 관리자 추가
3. @ExitSystem_bot 검색 및 추가
4. 권한: "메시지 전송" 체크
```

### Step 2: Parser Bot 채널 추가 (선택사항)

```
1. EXIT COMPANY 알림 채널 열기
2. 채널 정보 → 관리자 추가
3. Parser Bot 검색 및 추가
4. 권한: "메시지 전송" 체크
```

### Step 3: 채널 ID 확인

```bash
# Admin Bot을 채널에 추가한 후 채널에 테스트 메시지 전송
curl https://api.telegram.org/bot8482830575:AAGsth2J04_JWafIpPJOHPrCvvMc6aF2A6A/getUpdates | jq '.result[-1].channel_post.chat.id'

# 결과: -1001234567890 (음수, -100으로 시작)
```

### Step 4: Cloudflare 환경 변수 설정

```
TELEGRAM_CHANNEL_ID=[Step 3에서 확인한 채널 ID]
```

### Step 5: Admin Bot Webhook 업데이트

```bash
curl -X POST https://api.telegram.org/bot8482830575:AAGsth2J04_JWafIpPJOHPrCvvMc6aF2A6A/setWebhook \
  -H "Content-Type: application/json" \
  -d '{"url": "https://908ee40d.exit-company-system-5je.pages.dev/api/telegram/webhook/admin"}'
```

## 🧪 테스트

### 1. 채널 명령어 테스트

```
EXIT COMPANY 알림 채널에서 메시지 입력:

/help

→ 명령어 목록이 채널에 표시되면 성공!
```

### 2. 파싱 테스트

```
채널에 메시지 입력:

입금 100,000원 테스트

→ 입금 감지 알림이 채널에 표시되면 성공!
```

### 3. 자동 정산 테스트

```
채널에 메시지 입력:

/settle

→ 정산 결과가 채널에 표시되면 성공!
```

## 💡 장점

✅ **중앙 집중식**: 모든 정보가 하나의 채널에 집중
✅ **실시간 협업**: 팀원 모두가 같은 정보 공유
✅ **간편한 사용**: 별도 봇 DM 불필요
✅ **투명성**: 모든 작업이 채널에서 공개
✅ **즉시 실행**: 명령어 입력 즉시 결과 표시

## 📊 사용 시나리오

### 시나리오 1: 아침 업무 시작

```
채널에서:
/status → 어제 처리되지 않은 티켓 확인
/unconfirmed → 미확인 입금 확인
/pending → 대기중인 승인 확인
```

### 시나리오 2: 입금 발생

```
1. 파서가 자동으로 감지 및 DB 저장
2. 채널에 입금 알림 표시
3. 직원이 웹에서 회원 매칭
```

### 시나리오 3: 출금 발생

```
1. 파서가 자동으로 감지 및 DB 저장
2. 채널에 출금 알림 + 출처 확인 버튼 표시
3. 직원이 버튼으로 출처 선택
4. 자동으로 DB 업데이트
```

### 시나리오 4: 일일 정산

```
채널에서:
/settle → 배팅 자동 정산 실행
/bookkeep → 장부 자동 정리 실행

→ 결과가 채널에 리포트로 표시
```

## ⚠️ 주의사항

1. **채널 유형**: 반드시 **Private 채널**로 설정
2. **봇 권한**: Admin Bot과 Parser Bot 모두 "메시지 전송" 권한 필요
3. **채널 ID**: `-100`으로 시작하는 음수여야 함
4. **명령어**: `/`로 시작해야 함
5. **승인 권한**: 승인 버튼은 TELEGRAM_ADMIN_USER_ID만 사용 가능

## 🆘 문제 해결

### 명령어가 작동하지 않아요
1. Admin Bot이 채널 관리자로 추가되었는지 확인
2. TELEGRAM_CHANNEL_ID가 올바른지 확인
3. Webhook이 올바르게 설정되었는지 확인

### 파싱이 작동하지 않아요
1. Parser Bot이 채널 관리자로 추가되었는지 확인
2. 메시지 형식이 올바른지 확인 (예: "입금 100,000원 홍길동")
3. Parser Bot Webhook이 설정되었는지 확인

### 버튼이 작동하지 않아요
1. 관리자/직원 ID가 올바르게 설정되었는지 확인
2. Callback query 처리가 정상인지 로그 확인

## 📚 추가 문서

- **TELEGRAM_AUTOMATION_GUIDE.md**: 전체 자동화 시스템 가이드
- **TELEGRAM_2BOT_SETUP.md**: 기본 봇 설정 가이드

---

**모든 작업을 채널에서!** 🚀
